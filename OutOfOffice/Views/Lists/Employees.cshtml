@model List<Employee>
@{
    ViewData["Title"] = "Employee";
}

<div>
    <h1>Employees:</h1>
    @if (Model.Count<1)
    {
        <h3>List empty</h3>
    }
    else
    {
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">
                        @Html.ActionLink("Id", "Employees")
                    </th>
                    <th scope="col">
                        @Html.ActionLink("Full name", "Employees", new { sortBy = "FullName" })
                    </th>
                    <th scope="col">
                        @Html.ActionLink("Subdivision", "Employees", new { sortBy = "Subdivision" })
                    </th>
                    <th scope="col">
                        @Html.ActionLink("Position", "Employees", new { sortBy = "Position" })
                    </th>
                    <th scope="col">
                        @Html.ActionLink("Status", "Employees", new { sortBy = "Status" })
                    </th>
                    <th scope="col">
                        @Html.ActionLink("Partner", "Employees", new { sortBy = "Partner" })
                    </th>
                    <th scope="col">
                        @Html.ActionLink("Out Of Office Balance", "Employees", new { sortBy = "OutOfOfficeBalance" })
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var employee in Model)
                {
                    <tr>
                        <th scope="row">@employee.Id</th>
                        <td>@employee.FullName</td>
                        <td>@employee.Subdivision.ToString()</td>
                        <td>@employee.Position.ToString()</td>
                        <td>@employee.Status.ToString()</td>
                        <td>@(employee.PeoplePartner?.FullName ?? "No Partner")</td>
                        <td>@employee.OutOfOfficeBalance</td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>
